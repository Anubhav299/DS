#include <bits/stdc++.h>
using namespace std;

int main()
{
    int w,m, n, matches;
    vector<int> knights = {1,2,3,4,5};
    vector<int> dragons;
    //cin >> n;

    
    for(auto& it:knights)
    {
        if(it%2==0)
        {
            // knights.erase(find(knights.begin(),knights.end(),it));
            cout << it < " ";
        }
        
    }

}